<Page x:Class="wpf.addProduct"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="addProduct"
      FlowDirection="RightToLeft"
      Background="Transparent">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border Background="#CC222222" CornerRadius="20" Padding="30" Width="450" BorderBrush="#D4AF37" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="הוספת מוצר חדש למלאי" FontSize="26" Foreground="#D4AF37" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,15">
                        <TextBlock x:Name="NameProduct" Text="שם מוצר" Foreground="White" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtNameProduct" Height="35" Background="#33FFFFFF" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,15">
                        <TextBlock x:Name="Description" Text="תיאור מוצר" Foreground="White" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtDescription" Height="60" Background="#33FFFFFF" Foreground="White" BorderThickness="0" TextWrapping="Wrap" AcceptsReturn="True" Padding="5">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,15">
                        <TextBlock x:Name="price" Text="מחיר מוצר" Foreground="White" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtPrice" Height="35" Background="#33FFFFFF" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="1" Margin="10,0,0,15">
                        <TextBlock Text="שיוך לקטגוריה" Foreground="White" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbCategory" Height="35" DisplayMemberPath="CategoryName" SelectedValuePath="CategorId" Background="#33FFFFFF" Foreground="Black" BorderThickness="0" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <Button Content="הוסף מוצר למערכת" Background="#D4AF37" Foreground="Black" FontWeight="Bold" Height="45" Margin="0,15,0,0" Click="Button_Click" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Page>