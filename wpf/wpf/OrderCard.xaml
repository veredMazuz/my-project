<UserControl x:Class="wpf.OrderCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Button Click="Button_Click">
    <StackPanel>
            <TextBlock x:Name="orderName" Text="{Binding ShabbatParsha}" Foreground="Red" FontSize="14" HorizontalAlignment="Center"></TextBlock>

            <TextBlock x:Name="orderData" Text="{Binding OrderDate}" Foreground="Red" FontSize="14" HorizontalAlignment="Center"></TextBlock>
        
    </StackPanel>
    </Button>
</UserControl>
