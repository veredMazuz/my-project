<Page x:Class="wpf.ProductView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductView"
      FlowDirection="RightToLeft"
      Background="Transparent">

    <Page.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="LayoutTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1.3" ScaleY="1.3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

            
            <UniformGrid Grid.Row="0" x:Name="spCategoria" Rows="1" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" x:Name="txtCategoryButton" VerticalAlignment="Center">
                </StackPanel>
            </UniformGrid>
  

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <UniformGrid x:Name="uGridProducts" Columns="1">
            </UniformGrid>
        </ScrollViewer>

        <Button x:Name="btnCart" Grid.Row="2" Content="צפה בסל שלי והזמן עכשיו" 
                Background="#D4AF37" Foreground="Black" FontWeight="Bold" 
                Height="50" Margin="0,15,0,0" Click="Button_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Page>